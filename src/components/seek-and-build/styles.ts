import { css } from "@emotion/css";

const container = css`
    padding: 40px;
    background: #f6f5f5;
    > * {
        margin: 2rem 0;
    }
`;

const seekAndBuildImages = css`
    border-radius: 20%;
`;

export { seekAndBuildImages, container };
