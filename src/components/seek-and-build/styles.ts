import { css } from "@emotion/css";

const container = css`
    padding: 40px;
    background: #fff5ef;
    > * {
        margin: 2rem 0;
    }
`;

const seekAndBuildImages = css`
    border-radius: 20%;
`;

export { seekAndBuildImages, container };
