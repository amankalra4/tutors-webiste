import { css } from "@emotion/css";

const aboutUsImageContainer = css`
    height: 70vh;
    z-index: -1;
    > img {
        opacity: 0.85;
        object-fit: cover;
    }
`;

export { aboutUsImageContainer };
